name: Generate and Upload Zip Release

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Download ads.txt
      run: curl -o ads.txt https://blocklist.oli11gh.top/f/ads.txt

    - name: Create zip file
      run: zip $(date +"%d.%m.%y").zip ads.txt

    - name: Create release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.TOKEN }}
      with:
        tag_name: v0.1
        release_name: Release v0.1
        draft: false
        prerelease: false

    - name: Upload zip file to release
      id: upload-release-asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./$(date +"%d.%m.%y").zip
        asset_name: $(date +"%d.%m.%y").zip
        asset_content_type: application/zip
